%h1 Listing users

%table
  %tr
    %th Name
    - if current_user
      %th Email
    %th
    %th
  - @users.each do |user|
    %tr
      %td= user.name
      -if current_user
        %td= user.email
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)

