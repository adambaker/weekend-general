Our schedule has you participating in <%= pluralize @events.size, 'operation' %> today.

<% @events.each do |event| %>
<%= render "event_info_text", date: false, event: event %>

<% end %>

You have your orders.
